diff --git a/CMakeLists.txt b/CMakeLists.txt
index eddff22..82a1e45 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -66,6 +66,19 @@ find_package(Python REQUIRED COMPONENTS Interpreter Development)
 # A standard installation should not require these...
 if(NOT pxr_FOUND)
     find_package(pxr REQUIRED)
+
+    # USD 26.x monolithic export quirk: component targets (arch/tf/hd/...) are
+    # INTERFACE libs that never link the actual monolithic library (usd_m), so
+    # consumers get headers but no USD symbols (mass undefined references).
+    # Append usd_m to the root component 'arch' — every pxr component reaches it
+    # transitively, which puts libusd_m.so on all consumers' link lines.
+    if(TARGET usd_m AND TARGET arch)
+        # usd_m's interface reaches garch -> OpenGL::GL; make sure it exists here
+        if(NOT TARGET OpenGL::GL)
+            find_package(OpenGL REQUIRED)
+        endif()
+        set_property(TARGET arch APPEND PROPERTY INTERFACE_LINK_LIBRARIES usd_m)
+    endif()
 endif()
 
 # Our USD config doesn't specify TBB or Boost dependencies,
diff --git a/cmake/MoonshineUsdCompileOptions.cmake b/cmake/MoonshineUsdCompileOptions.cmake
index c557671..ae2abba 100644
--- a/cmake/MoonshineUsdCompileOptions.cmake
+++ b/cmake/MoonshineUsdCompileOptions.cmake
@@ -13,8 +13,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
                 -fno-strict-aliasing            # TODO: add a note
                 -fno-var-tracking-assignments   # Turn off variable tracking
                 -fpermissive                    # Downgrade some diagnostics about nonconformant code from errors to warnings.
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx                           # x86 options
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -pipe                           # Use pipes rather than intermediate files.
                 -pthread                        # Define additional macros required for using the POSIX threads library.
                 -w                              # Inhibit all warning messages.
@@ -38,7 +37,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -fdelayed-template-parsing      # Shader.h has a template method that uses a moonray class which is no available to scene_rdl2 and is only used in moonray+
                 -Wno-deprecated-declarations    # disable auto_ptr deprecated warnings from log4cplus-1.
                 -Wno-unused-value               # caused by opt-debug build and MNRY_VERIFY.
@@ -50,8 +49,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -Qoption,cpp,--print_include_stack
                 -fmerge-debug-strings
                 -fexceptions                    # Enable exception handling.
