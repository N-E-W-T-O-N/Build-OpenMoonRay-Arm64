diff --git a/cmake/MaterialxShadersCompileDefinitions.cmake b/cmake/MaterialxShadersCompileDefinitions.cmake
index 79a856b..82ddaa5 100644
--- a/cmake/MaterialxShadersCompileDefinitions.cmake
+++ b/cmake/MaterialxShadersCompileDefinitions.cmake
@@ -22,7 +22,7 @@ function(${PROJECT_NAME}_cxx_compile_definitions target)
             >
 
         PUBLIC
-            __AVX__
+            ${GLOBAL_CPP_FLAGS}     # __AVX__ on x86-64, __ARM_NEON__ on aarch64 (was hardcoded __AVX__)
             GL_GLEXT_PROTOTYPES=1                   # This define makes function symbols to be available as extern declarations.
             TBB_SUPPRESS_DEPRECATED_MESSAGES        # Suppress 'deprecated' messages from TBB
     )
diff --git a/cmake/MaterialxShadersCompileOptions.cmake b/cmake/MaterialxShadersCompileOptions.cmake
index a585a09..b49e27e 100644
--- a/cmake/MaterialxShadersCompileOptions.cmake
+++ b/cmake/MaterialxShadersCompileOptions.cmake
@@ -13,8 +13,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
                 -fno-strict-aliasing            # TODO: add a note
                 -fno-var-tracking-assignments   # Turn off variable tracking
                 -fpermissive                    # Downgrade some diagnostics about nonconformant code from errors to warnings.
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx                           # x86 options
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -pipe                           # Use pipes rather than intermediate files.
                 -pthread                        # Define additional macros required for using the POSIX threads library.
                 -w                              # Inhibit all warning messages.
@@ -38,7 +37,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -fdelayed-template-parsing      # Shader.h has a template method that uses a moonray class which is no available to scene_rdl2 and is only used in moonray+
                 -Wno-deprecated-declarations    # disable auto_ptr deprecated warnings from log4cplus-1.
                 -Wno-unused-value               # caused by opt-debug build and MNRY_VERIFY.
@@ -50,8 +49,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -Qoption,cpp,--print_include_stack
                 -fmerge-debug-strings
                 -fexceptions                    # Enable exception handling.
diff --git a/lib/map/CMakeLists.txt b/lib/map/CMakeLists.txt
index 28cd857..9f49d61 100644
--- a/lib/map/CMakeLists.txt
+++ b/lib/map/CMakeLists.txt
@@ -28,8 +28,8 @@ set_target_properties(
     PROPERTIES
         ISPC_HEADER_SUFFIX _ispc_stubs.h
         ISPC_HEADER_DIRECTORY /${relBinDir}
-        ISPC_INSTRUCTION_SETS avx2-i32x8
-        # ISPC_INSTRUCTION_SETS ${GLOBAL_ISPC_INSTRUCTION_SETS}
+        # use the platform's ISPC target (neon-i32x4 on aarch64) instead of hardcoded avx2
+        ISPC_INSTRUCTION_SETS ${GLOBAL_ISPC_INSTRUCTION_SETS}
         LINKER_LANGUAGE CXX
 )
 
