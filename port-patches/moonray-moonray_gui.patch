diff --git a/cmake/MoonrayGuiCompileOptions.cmake b/cmake/MoonrayGuiCompileOptions.cmake
index 59337cb..442b524 100644
--- a/cmake/MoonrayGuiCompileOptions.cmake
+++ b/cmake/MoonrayGuiCompileOptions.cmake
@@ -12,8 +12,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
                 -fno-omit-frame-pointer         # TODO: add a note
                 -fno-strict-aliasing            # TODO: add a note
                 -fpermissive                    # Downgrade some diagnostics about nonconformant code from errors to warnings.
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx                           # x86 options
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -pipe                           # Use pipes rather than intermediate files.
                 -pthread                        # Define additional macros required for using the POSIX threads library.
                 -w                              # Inhibit all warning messages.
@@ -37,7 +36,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -fdelayed-template-parsing      # Shader.h has a template method that uses a moonray class which is no available to scene_rdl2 and is only used in moonray+
                 -Wno-deprecated-declarations    # disable auto_ptr deprecated warnings from log4cplus-1.
                 -Wno-unused-value               # caused by opt-debug build and MNRY_VERIFY.
@@ -49,8 +48,7 @@ function(${PROJECT_NAME}_cxx_compile_options target)
         target_compile_options(${target}
             # TODO: Some if not all of these should probably be PUBLIC
             PRIVATE
-                -march=core-avx2                # Specify the name of the target architecture
-                -mavx
+                $<IF:$<STREQUAL:${CMAKE_SYSTEM_PROCESSOR},aarch64>,-march=armv8.2-a,-march=core-avx2>                # Specify the name of the target architecture
                 -Qoption,cpp,--print_include_stack
                 -fmerge-debug-strings
                 -fexceptions                    # Enable exception handling.
diff --git a/cmd/moonray_gui/CMakeLists.txt b/cmd/moonray_gui/CMakeLists.txt
index ee33633..41c584d 100644
--- a/cmd/moonray_gui/CMakeLists.txt
+++ b/cmd/moonray_gui/CMakeLists.txt
@@ -22,6 +22,15 @@ file(RELATIVE_PATH relBinDir ${PROJECT_SOURCE_DIR} ${crtBinDir})
 
 # TODO: actually check that the string produced by xxd matches the expectation. i.e. Test this beyond building it, which works
 # translate ".bin" source files to ".o" files
+# objcopy container format/arch must match the target (was hardcoded x86:
+# "architecture i386 unknown" on Linux aarch64) — same fix as pbr's sampler tables
+if(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64|arm64")
+    set(crtObjcopyArch aarch64)
+    set(crtObjcopyTarget elf64-littleaarch64)
+else()
+    set(crtObjcopyArch i386)
+    set(crtObjcopyTarget elf64-x86-64)
+endif()
 foreach(crtFile ${crtFiles})
     if(IsDarwinPlatform)
         add_custom_command(
@@ -33,8 +42,8 @@ foreach(crtFile ${crtFiles})
             OUTPUT  ${CMAKE_CURRENT_BINARY_DIR}/${crtFile}.o
             DEPENDS ${crtBinDir}/${crtFile}.bin
             COMMAND cd ${PROJECT_SOURCE_DIR} &&
-                        objcopy --input-target=binary --binary-architecture=i386
-                        --output-target=elf64-x86-64
+                        objcopy --input-target=binary --binary-architecture=${crtObjcopyArch}
+                        --output-target=${crtObjcopyTarget}
                         "${relBinDir}/${crtFile}.bin"
                         "${CMAKE_CURRENT_BINARY_DIR}/${crtFile}.o")
     endif()
@@ -103,5 +112,15 @@ if (NOT OpenColorIO_FOUND)
     target_compile_definitions(${target} PRIVATE DISABLE_OCIO)
 endif()
 
+# Qt defines `emit` as an empty macro; oneTBB (2021+) has `void emit()` in
+# oneapi/tbb/profiling.h, so any TU that includes Qt before TBB fails with
+# "expected unqualified-id before ')'". Force-include the TBB header first in
+# every TU (including generated MOC files) so its method is parsed before the
+# macro exists. Upstream doesn't hit this because it pins TBB 2020.
+# QtQuirks.h handles the sibling `slots`/`signals` clashes the same way.
+if(TARGET TBB::tbb)
+    target_compile_options(${target} PRIVATE -include oneapi/tbb/profiling.h)
+endif()
+
 install(TARGETS ${target}
     RUNTIME DESTINATION bin)
diff --git a/cmd/moonray_gui/RenderGui.h b/cmd/moonray_gui/RenderGui.h
index 0ec9694..b5522d3 100644
--- a/cmd/moonray_gui/RenderGui.h
+++ b/cmd/moonray_gui/RenderGui.h
@@ -10,7 +10,7 @@
 #include <moonray/rendering/rndr/rndr.h>
 #include <scene_rdl2/common/grid_util/ShmFbOutput.h>
 
-#include <tbb/atomic.h>
+#include <atomic> // tbb::atomic/tbb/atomic.h removed in oneTBB 2021+
 
 #define NUM_TILE_FADE_STEPS  4
 
@@ -123,7 +123,7 @@ private:
     /// The renderering code will strive to render this frame. If it's rendering
     /// a frame with a lower timestamp then we know the frame it's currently
     /// rendering is old.
-    tbb::atomic<uint32_t>   mMasterTimestamp;
+    std::atomic<uint32_t>   mMasterTimestamp;
 
     /// The timestamp of the frame the renderer is currently processing.
     uint32_t                mRenderTimestamp;
diff --git a/cmd/moonray_gui_v2/viewport/SnapshotManager.cc b/cmd/moonray_gui_v2/viewport/SnapshotManager.cc
index 9b309ac..ab13f45 100644
--- a/cmd/moonray_gui_v2/viewport/SnapshotManager.cc
+++ b/cmd/moonray_gui_v2/viewport/SnapshotManager.cc
@@ -20,8 +20,40 @@
 #define OIIO_NO_SSE 1
 #define OIIO_NO_AVX 1
 #define OIIO_NO_AVX2 1
+// GCC on Linux aarch64: the include-guard trick above only works for clang
+// (GCC's immintrin guard differs and the header doesn't even exist on arm).
+// OIIO's vendored farmhash.h does `#if defined(__SSSE3__) ... #include
+// <immintrin.h>`, and Platform.hh deliberately defines the SSE macros on
+// aarch64 (ISA masquerade for sse2neon). Undefine them here: moonray headers
+// above are already preprocessed, and OIIO must take its generic paths.
+#undef __SSE__
+#undef __SSE2__
+#undef __SSE3__
+#undef __SSSE3__
+#undef __SSE4_1__
+#undef __SSE4_2__
+#undef __AVX__
+#undef __AVX2__
 #endif
 #include <OpenImageIO/imageio.h>
+// Restore Platform.hh's aarch64 ISA masquerade (undef'd above only so OIIO
+// headers take generic paths). Later headers dispatch on these (geom/Types.h).
+#if defined(__aarch64__)
+  #ifndef MOONRAY_ISA_NEON2X
+    #ifndef __SSE3__
+    #define __SSE3__
+    #endif
+    #ifndef __SSSE3__
+    #define __SSSE3__
+    #endif
+    #ifndef __SSE4_1__
+    #define __SSE4_1__
+    #endif
+    #ifndef __SSE4_2__
+    #define __SSE4_2__
+    #endif
+  #endif
+#endif
 
 using namespace moonray;
 using namespace scene_rdl2::logging;
