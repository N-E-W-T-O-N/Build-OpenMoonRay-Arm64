diff --git a/cmd/mcrt_cmd/shmFbDump/main.cc b/cmd/mcrt_cmd/shmFbDump/main.cc
index 949ff94..c8e8697 100644
--- a/cmd/mcrt_cmd/shmFbDump/main.cc
+++ b/cmd/mcrt_cmd/shmFbDump/main.cc
@@ -9,23 +9,25 @@
 #include <sstream>
 #include <sys/shm.h>
 
-#ifdef __APPLE__
+#if defined(__aarch64__) // any aarch64 (was __APPLE__ — broke Linux arm64)
 #include <arm_neon.h>
-#else // else __APPLE__
-#ifdef __INTEL_COMPILER 
+#else // x86
+#ifdef __INTEL_COMPILER
 // We don't need any include for half float instructions
 #else // else __INTEL_COMPILER
 #include <x86intrin.h>          // _mm_cvtps_ph, _cvtph_ps : for GCC build
 #endif // end !__INTEL_COMPILER
-#endif 
+#endif
 
 float    
 h16tof32(const unsigned short h)
 {
 #if defined(__ARM_NEON__)
-	float output;
-	vst1q_f32(&output, vcvt_f32_f16(vld1_u16(&h)));
-	return output;
+    // GCC needs the explicit u16->f16 reinterpret; also the old code
+    // vst1q_f32'd 4 floats into a single float (out-of-bounds write) —
+    // extract lane 0 instead.
+    const float16x4_t h4 = vreinterpret_f16_u16(vdup_n_u16(h));
+    return vgetq_lane_f32(vcvt_f32_f16(h4), 0);
 #else
     return _cvtsh_ss(h); // Convert half 16bit float to full 32bit float
 #endif
diff --git a/lib/common/grid_util/PackTiles.cc b/lib/common/grid_util/PackTiles.cc
index 2f49b2d..b7f37fc 100644
--- a/lib/common/grid_util/PackTiles.cc
+++ b/lib/common/grid_util/PackTiles.cc
@@ -53,13 +53,13 @@
 #endif // end DEBUG_MODE
 
 
-#ifndef __APPLE__
-#ifdef __INTEL_COMPILER 
+#if defined(__aarch64__)
+#include <arm_neon.h>           // vcvt_f32_f16 etc. for the NEON half-float paths
+#elif defined(__INTEL_COMPILER)
 // We don't need any include for half float instructions
-#else // else __INTEL_COMPILER
+#else
 #include <x86intrin.h>          // _mm_cvtps_ph, _cvtph_ps : for GCC build
-#endif // end !__INTEL_COMPILER
-#endif 
+#endif
 
 //#define DEBUG_MSG_SIZEDUMP
 
@@ -608,10 +608,11 @@ private:
 
     inline static unsigned short ftoh(const float f)
     {
-#if defined(__ARM_NEON__)   // TODO: Verify this
-	__fp16 output;
-	vst1_f16(&output, vcvt_f16_f32(vld1q_f32(&f)));
-	return output;
+#if defined(__ARM_NEON__)
+        // verified impl: dup the scalar into a vector, convert, extract lane 0.
+        // (The previous code vld1q_f32'd 4 floats from &f — out-of-bounds read.)
+        const float16x4_t h4 = vcvt_f16_f32(vdupq_n_f32(f));
+        return vget_lane_u16(vreinterpret_u16_f16(h4), 0);
 #else
         return _cvtss_sh(f, 0); // Convert full 32bit float to half 16bit float
                                 // An immediate value controlling rounding using bits : 0=Nearest
@@ -620,10 +621,9 @@ private:
 
     inline static float htof(const unsigned short h)
     {
-#if defined(__ARM_NEON__)   // TODO: Verify this
-	float output;
-	vst1q_f32(&output, vcvt_f32_f16(vld1_u16(&h)));
-	return output;
+#if defined(__ARM_NEON__)
+        const float16x4_t h4 = vreinterpret_f16_u16(vdup_n_u16(h));
+        return vgetq_lane_f32(vcvt_f32_f16(h4), 0);
 #else
         return _cvtsh_ss(h); // Convert half 16bit float to full 32bit float
 #endif
